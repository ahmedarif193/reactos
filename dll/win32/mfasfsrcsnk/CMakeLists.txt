spec2def(mfasfsrcsnk.dll mfasfsrcsnk.spec)

set(MFSRCSNK_DIR ${REACTOS_SOURCE_DIR}/dll/win32/mfsrcsnk)
set_source_files_properties(mfasfsrcsnk.rc PROPERTIES OBJECT_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/mfasfsrcsnk.rgs)
add_library(mfasfsrcsnk MODULE ${MFSRCSNK_DIR}/media_source.c mfasfsrcsnk.c mfasfsrcsnk.rc ${CMAKE_CURRENT_BINARY_DIR}/mfasfsrcsnk.def ${CMAKE_CURRENT_BINARY_DIR}/mfasfsrcsnk_stubs.c)
add_idl_reg_scripts(mfasfsrcsnk registry mfasfsrcsnk.idl)
target_include_directories(mfasfsrcsnk PRIVATE ${MFSRCSNK_DIR})
set_module_type(mfasfsrcsnk win32dll)
target_compile_definitions(mfasfsrcsnk PRIVATE __WINESRC__)
target_link_libraries(mfasfsrcsnk mfuuid uuid wine wine_dll_register)
add_importlibs(mfasfsrcsnk advapi32 ole32 mfplat winedmo msvcrt kernel32 ntdll)
add_cd_file(TARGET mfasfsrcsnk DESTINATION reactos/system32 FOR all)
set_wine_module(mfasfsrcsnk)
