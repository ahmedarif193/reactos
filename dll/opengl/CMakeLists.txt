
add_subdirectory(glu32)
add_subdirectory(mesa)
add_subdirectory(opengl32)
if(MESA_GALLIUM_FROM_SOURCE)
    if(EXISTS "${REACTOS_SOURCE_DIR}/submodules/mesa/CMakeLists.txt")
        add_subdirectory(mesa_gallium)
    else()
        message(STATUS "Mesa Gallium: the mesa feed is not checked out; skipping it. "
            "Run scripts/feeds update mesa to build it.")
    endif()
endif()
if((ARCH STREQUAL "arm64") AND RPI3_SUPPORT)
    add_subdirectory(rpi3vc4ogl)
endif()
if((ARCH STREQUAL "arm64") AND RPI5_SUPPORT)
    add_subdirectory(rpi5vc4ogl)
endif()
